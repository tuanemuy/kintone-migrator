# 動作確認計画 — Issue #130: SpaceApp 型を汎用的な名前にリネーム

**Issue:** #130
**作成日:** 2026-04-02

---

## 確認環境

- ローカル開発環境（Node.js 22.x, pnpm）
- テスト用の kintone 環境（`pnpm dev` コマンドの実行確認用）

## 確認項目

### 1. 型チェック・lint・テストの通過

- **目的:** 機械的リネームに漏れがないことを確認する
- **手順:**
  1. `pnpm typecheck` を実行する
  2. `pnpm lint:fix` を実行する
  3. `pnpm format` を実行する
  4. `pnpm test` を実行する
- **期待結果:** 全てエラーなしで完了する
- **確認ポイント:** 特に `resolveAppName` のテスト（`app/__tests__/entity.test.ts`）が全ケース合格すること

### 2. `SpaceApp` の残存チェック

- **目的:** リネーム漏れがないことを確認する
- **手順:**
  1. `grep -r "SpaceApp" src/` を実行する
- **期待結果:** ヒットなし（`.issue/` 配下の計画ファイルは除く）
- **確認ポイント:** コメント中の言及も含めて確認する

### 3. `init` コマンドの動作確認（スペースID指定あり）

- **目的:** スペースIDを指定した `init` コマンドが正常に動作することを確認する
- **手順:**
  1. `pnpm dev -- init --base-url <URL> --api-token <TOKEN> --space-id <SPACE_ID>` を実行する
  2. アプリ一覧が表示され、選択UIが正常に動作することを確認する
  3. プロジェクト設定ファイルが正しく生成されることを確認する
- **期待結果:** Issue #128 以前と同じ動作（型のリネームのみでロジック変更なし）
- **確認ポイント:** アプリ名の解決（`resolveAppName`）が正常に動作すること（code がある場合は code、ない場合は name が使われる）

### 4. `init` コマンドの動作確認（スペースID指定なし）

- **目的:** スペースID未指定の `init` コマンドが正常に動作することを確認する
- **手順:**
  1. `pnpm dev -- init --base-url <URL> --api-token <TOKEN>` を実行する（`--space-id` なし）
  2. 全アプリ一覧が表示され、選択UIが正常に動作することを確認する
- **期待結果:** Issue #128 で追加された機能が引き続き正常動作する
- **確認ポイント:** `fetchAllApps` 経由のアプリ取得が正常に動作すること

## エッジケース・異常系

### 1. ビルドの動作確認

- **目的:** プロダクションビルドが正常に完了することを確認する
- **手順:**
  1. `pnpm build` を実行する
- **期待結果:** エラーなしでビルド完了

## 既存機能への影響確認

- `init` コマンド以外のサブコマンド（`schema diff`, `schema migrate` 等）は `SpaceApp` 型を使用していないため、影響なし。`pnpm test` の全テスト通過で既存機能の非退行を確認する。

## 確認チェックリスト

- [ ] `pnpm typecheck` — エラーなし
- [ ] `pnpm lint:fix` — エラーなし
- [ ] `pnpm format` — エラーなし
- [ ] `pnpm test` — 全テストパス
- [ ] `grep -r "SpaceApp" src/` — ヒットなし
- [ ] `pnpm build` — ビルド成功
- [ ] `init` コマンド（スペースID指定あり）— 正常動作
- [ ] `init` コマンド（スペースID指定なし）— 正常動作
